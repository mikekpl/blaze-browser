[toolchain]
# Latest stable. servo v0.5.0 (MSRV 1.88) builds on it and the real-engine
# smoke test (blaze-engine-servo/tests/embedded_smoke.rs) passes.
channel = "1.98.1"
components = ["clippy", "rustfmt"]
