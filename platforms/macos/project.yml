name: Blaze
options:
  bundleIdPrefix: dev.blaze
  deploymentTarget:
    macOS: "13.0"
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "5.10"
    MACOSX_DEPLOYMENT_TARGET: "13.0"
    ENABLE_HARDENED_RUNTIME: YES
    DEAD_CODE_STRIPPING: YES

targets:
  Blaze:
    type: application
    platform: macOS
    sources:
      - path: Blaze
      - path: Generated
        includes:
          - "*.swift"
      - path: ../../assets
        buildPhase: resources
        type: folder
    dependencies:
      - framework: Generated/BlazeFFI.xcframework
        embed: false
    # Every build gets its own semver (scripts/version.sh), stamped into the
    # built Info.plist before code signing.
    postBuildScripts:
      - name: Stamp build version
        script: '"$SRCROOT/../../scripts/version.sh" --stamp "$TARGET_BUILD_DIR/$INFOPLIST_PATH"'
        basedOnDependencyAnalysis: false
        # orders the phase after Xcode has generated the plist (clean builds)
        inputFiles:
          - $(TARGET_BUILD_DIR)/$(INFOPLIST_PATH)
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.blaze.Blaze
        # the stamp phase runs git and edits the built plist
        ENABLE_USER_SCRIPT_SANDBOXING: NO
        INFOPLIST_KEY_NSHumanReadableCopyright: ""
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.productivity
        GENERATE_INFOPLIST_FILE: YES
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_ENTITLEMENTS: Blaze/Blaze.entitlements
        CODE_SIGN_IDENTITY: "-"
        LIBRARY_SEARCH_PATHS: "$(inherited)"
        SWIFT_OBJC_INTEROP_MODE: objc
        OTHER_LDFLAGS: "-lc++"

  BlazeTests:
    type: bundle.unit-test
    platform: macOS
    sources: [BlazeTests]
    dependencies:
      - target: Blaze
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.blaze.BlazeTests
        GENERATE_INFOPLIST_FILE: YES
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/Blaze.app/Contents/MacOS/Blaze"
        BUNDLE_LOADER: "$(TEST_HOST)"

  BlazeUITests:
    type: bundle.ui-testing
    platform: macOS
    sources: [BlazeUITests]
    dependencies:
      - target: Blaze
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.blaze.BlazeUITests
        GENERATE_INFOPLIST_FILE: YES
        TEST_TARGET_NAME: Blaze

schemes:
  Blaze:
    build:
      targets:
        Blaze: all
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - BlazeTests
        - BlazeUITests
